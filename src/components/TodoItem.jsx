const TodoItem = ({ text, completed }) => {
    return (
        <>
            <div className={`${completed ? 'todo-item todo-completed':'todo-item'}`}>
                fe
            </div>
        </>
    )
}
export { TodoItem }
